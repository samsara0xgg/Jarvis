# Jarvis Realtime — 用户体验与完成合同

## Goal

由当前 Astra 主任务接续已有实现，完成 Jarvis 自研 Realtime：Allen 能自然
交谈、及时听到有用回答、打断说话并继续提问，在后台任务运行时继续使用
Jarvis，且语音、屏幕、真实动作与恢复后的状态一致。保留回答质量、证据、
授权与六层架构；不接入 OpenAI Realtime API。

本合同采用 2026-09-04 Allen 要求的目标驱动工作方式。实施路线是可修正的
工作假设，不是验收合同；既有 Wave 名称和步骤不作为完成依据。

## Done

- 普通对话：真实麦克风 → ASR → L3 → 安全输出 → TTS → 实际播放闭环；
  热启动普通回合首个有意义的可听回答 p50 ≤ 2.0 s、p95 ≤ 3.5 s。
  样本、设备、模型、网络、冷/热条件和失败样本全部记录，不能只选最好一次。
- 安全流式：普通低风险回答在全文生成结束前开始输出；高风险、未知或证据
  不足的片段不提前放行。text/tool/usage/error/cancel 协议完整；短语音与
  长文具有独立生成/取消寿命，已播放前缀不能被悄悄改写。
- 交谈与打断：唯一音频输入 owner；PTT、keyword 与适用设备的自然打断有
  真实验收。“别说了”不取消动作；控制短语不触发多余回答；已听到的保守
  前缀进入后续上下文。partial ASR、semantic endpoint 与设备变化安全降级
  满足既有 ADR 的产品条件；播放停止须区分软件、估计 DAC 和物理证据。
- 后台工作：长任务运行时第二、第三个输入仍可处理；有依据的可听反馈在
  dispatch 后 1.5 s 内出现。结果接续不重不漏、不抢新对话；明确取消任务
  后实际进程、资源与清理状态一致。默认 worker 被长 turn 占满不是完成。
- 安全与恢复：确认不双花，取消竞态不放过未准入请求，成本不重记不漏记，
  同资源冲突安全处理。重启恢复输入、待派发记录和结果接续；执行与否不明
  时保持明确的未决状态，禁止盲目重放副作用或假称成功。
- Inherent：Swift 源码纳入同仓；v2 协议、状态 reducer、流控、snapshot、
  重连及停止/PTT/取消/确认控件端到端可用。屏幕长文不阻塞语音，慢客户端
  不阻塞控制；重连不重播音频，任务与确认状态不靠猜测重建。
- 质量与交付：既有功能回归、六层约束、相关静态检查及独立验收通过；质量
  eval 不劣于固定的旧路径基线。交付可运行的已验证 revision、启动/回退方式
  和简洁证据索引。最终代码修改后重验受影响的完成条件，历史版本通过不能
  代替当前版本通过。生产默认切换单独记录，不能把未部署描述为已上线。

耳机、无 AEC 扬声器、有 AEC 扬声器按 ADR-0006 各自验收；未达标 profile
保持安全降级并列为未完成。不能通过删掉高级全双工范围宣称整个目标完成。

## Constraints and non-goals

- 继续使用现有最小可行实现；不重写 Jarvis、不顺手重构无关模块。
- runtime 是跨层装配入口；L3 拥有语义/安全决策，L2 拥有持久真相。
- ADR 只记录新的持久架构决定，或修正已经改变的持久合同；不扩写逐文件、
  逐函数实现步骤。已有 ADR 的具体实现设想允许调整，产品语义与安全边界
  若需改变必须明确提出，不能悄悄降低验收标准。
- 隔离实现，保护 main 的现有未提交文件与其他任务现场。不 push，不改生产
  数据或生产默认设置；不擅自执行不可逆操作。沿用仓库的提交与测试规则。
- 本合同授权日常可逆实施选择；不扩张云端数据传输授权。已有 live 请求的
  目标/payload 阻断先读准确失败记录，优先用明确授权范围内的合成夹具解决。
  确实需要用户数据、设备参与或新增外部授权时，只问无法自行恢复的具体信息。

## Autonomy and state

- 中央枢纽为任务 01a06eb3-13e0-7291-8536-b19da3f1dda8，负责目标、必要决策、
  任务协调与交付；每个新大目标使用一个新的 Astra 实现任务，全部生产代码
  实现和返修留在该任务。当前大目标不按 Wave 拆成多个用户任务。
- 实现任务完成实现与自验、提交固定候选 SHA 后，枢纽再创建全新的独立验证
  任务。验证任务依据 Goal 和实际代码独立取证，不继承实现方推理、不改生产
  代码；失败经枢纽转回原实现任务，修复后由验证任务复验受影响条件。
- 枢纽仅保存任务映射、关键决定、Done 状态、版本和证据索引；长日志与实现
  细节留实现/验证任务。双方有重大阻断或完成事件时主动向枢纽发简报。
- 主任务持续负责实现与整合；不在每个 commit、Wave 或普通可逆选择后等批准。
- 子 agent 用于有界调查、日志压缩、独立反例/验收，或确实互不依赖的工作；
  不让多个写入者同时修改共享核心文件。
- GOAL.md 保存合同；progress.md 只保留当前 checkpoint、完成条件状态、关键
  发现、失败原因、证据位置与下一待解问题。Git 保存经过验证的逻辑提交。
- 恢复工作时先读合同、当前状态和相关 diff，做与现状相称的 smoke check；
  不按固定 context 百分比清空，不重复已完成的研究或机械重跑所有检查。
- 只有全部 Done 有证据时才报告整体完成。局部完成、外部阻断和未验收分别
  标明；不能把通过测试、代码复审或功能开关关闭当成产品验收完成。

## Starting checkpoint

2026-09-04：设计已获批；最新累积实现为 codex/wave45-takeover-fix 的
86958ad（已验证代码 3a6856a）。独立非 live 记录 442 passed / 63 deselected，
静态检查通过；最新 live/E2E 未完成，默认开关关闭，尚未合入 main。
该 worktree 的旧 GOAL.md 仅描述已收尾的安全修复，不能当作本次总体目标。
本文件已建立新的总体合同；尚未据此开始新的生产代码修改或重新执行 live。

# Python
__pycache__/
*.py[cod]
*.so

# Virtual env
.venv
venv/

# Test / lint caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Runtime state
var/
*.db
*.db-*

# Per-run Tier 2 artifacts (token-use JSON, scenario LLM logs).
tests/_artifacts/

# Voice model assets — per-machine symlinks into external checkouts /
# downloads, never committed (distribution contract: models are located
# via explicit external paths, not repo data). Keeps `git status` clean.
/data/sensevoice-small-int8
/data/silero_vad.onnx

# Secrets
.env
.env.*

# Build
build/
dist/
*.egg-info/

# OS
.DS_Store
*.pyc
.ipynb_checkpoints/

# Local agent / draft / eval (per-machine, not committed)
.claude/*
!.claude/rules/
!.claude/skills/
!.claude/agents/
Harness/
evals/
AGENTS.md

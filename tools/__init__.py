"""Worktree-root developer tools (not part of the `jarvis` package).

Scripts under `tools/` live outside `jarvis/` and are therefore exempt from the
`.importlinter` layer DAG. They exist for ad-hoc debugging — see individual
module docstrings for purpose.
"""

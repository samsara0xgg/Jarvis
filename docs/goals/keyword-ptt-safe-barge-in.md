# Goal: keyword-ptt-safe-barge-in

## Goal
While Jarvis is speaking, a confirmed user interrupt — a PTT upload, or the wake phrase followed by a configured interrupt keyword recognized on the partial-ASR path inside `candidate_window_ms` when `allowed_barge_mode` is `keyword_two_stage` — cancels the speaking response through the existing generation-scope cancel callable with reason `barge_in`, under that run's own `ResponseInterruptPolicy`, while speaker echo, a wake hit alone, and `output_active` alone can never cancel, and `natural` mode stays unreachable.

## Why
ADR-0006 D8 is the barge-in contract and none of it exists: the voice session's only reaction to active output is to suppress wake detections. D9's mode table and F12 allow exactly two safe stop paths without validated AEC — PTT, and wake-phrase-plus-interrupt-keyword — and this card builds those two and nothing else.

## Current behavior
Base: `realtime-integration` at `ca1b42f` (merge of lane/a). Cards `endpointing-partial-asr` (B1, merged — the partial-ASR lane is present) and `device-profile-resolver` (B2) are the lane-B predecessors; B2 must be merged into this card's base — if `DeviceProfileSnapshot.allowed_barge_mode` is absent when work starts, the card contradicts the repository: stop and report.

- No barge-in code exists anywhere. `grep -rn "InterruptUtteranceRouter\|BargeInSignal\|BargeInRouter\|voice_interrupt\|DeviceProfileSnapshot\|allowed_barge_mode" jarvis/ tests/ scripts/ config/` returns nothing, and `jarvis/surface/` has no `voice_interrupt.py`.
- The only cancel path is HTTP: `POST /inherent/cancel-response` (jarvis/surface/inherent_server.py:643, dep field `cancel_response_callable` :306, documented :287-297) → `make_response_cancel_callable` (jarvis/runtime/__init__.py:1646-1708, built at jarvis/runtime/inherent_loop.py:3000-3004 and injected into `InherentDeps` at :3026) → `request_response_cancel` (jarvis/decision/response_run.py:693). That function rejects every scope but `generation` as `unsupported_scope` (:714-715) because no playback lease exists (docstring :700-707; ADR-0008 D10 :609-613) — true when this card was written; `foreground_output` has since shipped (merge fd01fe4) and is no longer among the rejected scopes.
- `RESPONSE_CANCEL_REASONS` is the closed set `{user_stop, superseded, shutdown, operator_request}` (jarvis/shared/realtime.py:23-25, docstring citing "ADR-0008 D10, Q7" :26-31). `make_response_cancel_callable` normalizes anything outside it to `operator_request` and logs a warning (jarvis/runtime/__init__.py:1678-1685). **`barge_in` is not in the set today.**
- `ResponseInterruptPolicy` exists but nothing reads it: `confirmed_playback` defaults to `"ignore"` (jarvis/shared/realtime.py:202), `action_action` is `Literal["never"]` (:204), and `start_response_run` constructs the policy with defaults only (jarvis/decision/response_run.py:683-686). Its single production caller is jarvis/runtime/__init__.py:1601, where runtime config is in scope.
- `ResponseRunRegistry` (jarvis/decision/response_run.py:387-414) is the only live-run index. `get()` (:400-403) is what the cancel entry point uses; `open_runs()` (:410-414) returns every non-terminal run and is the only existing way to name a currently-speaking response without adding an accessor to the media owner.
- The session's only "is output active" signal is the injected `output_active` callable (jarvis/surface/voice_session.py:853, stored :862, called :1038-1039), bound in production to `StreamingTTSPipeline.is_output_active` (jarvis/runtime/inherent_loop.py:2134). Its sole use is to suppress wake detections: the decision block at :1036-1069 increments `wake_suppressed_during_output` (:1047) and traces `audio_input_wake_suppressed` with `reason="wave3_no_interrupt_policy_during_output"` and `hard_cancel_performed=False` (:1048-1055) instead of taking the else-arm (:1056-1069) that enqueues a `WakeDetection`. The module docstring (:7-11) states Wave 3 deliberately has no barge-in cancellation path.
- The partial-ASR lane only produces text while capture is armed: `PartialAsrLane` is built when `partial_asr.enabled` (jarvis/surface/voice_session.py:871), its thread starts at :968, and revisions are consumed inside `UtteranceAssembler._pull_revisions` (:772-802), which normalizes each revision with `voice_asr.normalize_partial_text` (jarvis/surface/voice_asr.py:695-698 — NFKC + casefold + whitespace collapse) at :786 and traces `asr_partial` (:792-799). Arming happens only via the `_detections` queue → `_drain_detection_commands` (def at :1109) → `UtteranceAssembler.arm`. **So during output nothing is armed and no partial text exists today.**
- `_interrupt_active` (jarvis/surface/voice_media.py:2721) is the real stop — tombstone, cancel provider I/O, durable `surface.playback_interrupted`, release — and now has four callers: :1403 (`system_sleep`), :2028 (`foreground_superseded`), :3059 (shutdown), and :1935 inside `_response_terminal`, which lane A's incremental-tts card wired to fire on `response.cancelled` / `response.failed` (`_RESPONSE_TERMINAL_TYPES`, voice_media.py:68). That wiring has landed; this card consumes it as an already-merged dependency rather than waiting on it. `StreamingTTSPipeline` has no external stop-speech entry and no accessor for the active response id (`_ActiveResponse` :356-372).
- Utterances reach `decide()` with no priority lane: the intent pump polls `utterance.received` rows into one bounded `asyncio.Queue` (jarvis/runtime/inherent_loop.py:969).
- PTT is a whole-WAV upload, `POST /inherent/asr-submit` (route decorator jarvis/surface/inherent_server.py:720, handler ends :731, body `_run_asr_submit` def :515 to :597, channel `"inherent_ptt"` :575). **There is no press/release signal on the wire** — not in `InherentDeps` (no field sits between `voice_pipeline_callable` :305 and `cancel_response_callable` :306) and not in the Swift bridge; the upload is the only observable PTT act.
- `realtime.single_audio_ingress` (config/jarvis.yaml:232-278, `partial_asr` sub-block :264-269) already has a `barge_in` block (:276-278, added by `device-profile-resolver`, merged f16f77b) with keys `detection_mode` and `accepted_natural_profiles`, sitting after `route_observer` (:274-275). This card EXTENDS that existing block with its own keys (`enabled`, `candidate_window_ms`, `confirm_timeout_ms`, `interrupt_keywords`) and must not create a second block or move the existing one. Both parsers read keys individually and ignore unknown ones — `audio_ingress_config_from_mapping` (jarvis/surface/voice_audio.py:2503) and `realtime_input_session_config_from_mapping` (jarvis/surface/voice_session.py:1321, sub-parser `_partial_asr_config_from_mapping` :1390) — so B2's and this card's `barge_in` subkeys can be parsed by different parsers from one YAML block without either rejecting the other's keys. `realtime.response.cancel_timeout_ms` is `500` (config/jarvis.yaml).
- Existing safety pins: `tests/canary/test_canary_audio_ingress_realtime_safety.py:83-98` (`test_wave3_input_session_has_no_hard_cancel_or_system_mute_authority`, a forbidden-token scan over `voice_session` source plus the two trace-string assertions) and `tests/integration/test_wave3_single_audio_ingress.py:1660-1691` (`output_active=lambda: True`, 12 frames, asserts `pipeline.calls == []` and suppression counted).
- Hermetic baseline at `5a75f2a`: `PYTHONPATH=. .venv/bin/python -m pytest -q -m "not live_llm and not live_codex"` → **912 passed, 64 deselected**. That already includes B2; the binding number is the integration count measured at launch plus this card's new tests.
- The daemon cannot take a replay input backend: `_single_ingress_activation` refuses anything but `backend: sounddevice` with `reason="unsupported_input_backend"` (jarvis/runtime/inherent_loop.py:1971-1976). B1's `FileReplayBackend` is reached only through an in-process composition root, the shape `scripts/replay_endpointing.py:123-172` already uses.

## Target behavior
- **Two spoken stages (ADR-0006 D9 :466).** A new L5 module `jarvis/surface/voice_interrupt.py` owns `BargeInPhase`, `BargeInSignal`, the candidate window, the keyword matcher, and `BargeInRouter`. There is no VAD-candidate stage: no capture frame, energy verdict, or Silero probability can ever open or confirm a barge-in.
  - **Candidate = a wake hit during output.** While `output_active()` is true, `barge_in.enabled`, and `allowed_barge_mode` is `keyword_two_stage`, the wake decision block (jarvis/surface/voice_session.py:1036-1069) no longer drops the detection: it traces `barge_in_candidate`, opens a `candidate_window_ms` window, and arms capture through the existing `_detections` path so the partial-ASR lane runs. Nothing is ducked, nothing is cancelled, and no durable event is written by the candidate.
  - **Confirm = the interrupt keyword on the partial-ASR path.** A partial revision whose normalized text contains any `barge_in.interrupt_keywords` entry, arriving inside an open window, emits exactly one `BargeInSignal(phase="confirmed", confirm_source="keyword")` and the trace `barge_in_confirmed`. Both sides are normalized with the normalizer the partial path already applies (`voice_asr.normalize_partial_text`, jarvis/surface/voice_asr.py:695-698); the keyword match reads the normalized text `UtteranceAssembler._pull_revisions` (jarvis/surface/voice_session.py:786) already computes.
  - **Confirm = a PTT upload during output.** An `/inherent/asr-submit` upload arriving while output is active confirms directly, with no prior candidate and in both modes, as `BargeInSignal(phase="confirmed", confirm_source="ptt")`. A button press is not echo.
  - A window that reaches `candidate_window_ms` with no keyword is dropped as typed telemetry — trace `barge_in_candidate_dropped` plus a counter on `VoiceSessionMetrics` — and produces no cancel (ADR-0006 §6 F11 :651).
  - With barge-in off, or in `ptt` mode, the wake hit stays suppressed exactly as today: same counter, same `audio_input_wake_suppressed` trace, nothing armed.
  - Fail closed on a missing prerequisite: if `allowed_barge_mode` is `keyword_two_stage` while `partial_asr.enabled` is false, the keyword confirm is unavailable — log one warning and accept PTT confirms only.
- **The router is closed and injected.** `BargeInRouter` receives `BargeInSignal`s, deduplicates per candidate window, and calls one injected `interrupt: Callable[[str], str]` bounded by `confirm_timeout_ms`, recording the returned outcome on `barge_in_confirmed`. It never imports `jarvis.decision` or `jarvis.runtime`, never touches actions, and never writes a durable event. `jarvis/surface/voice_session.py` gains only a config field, an optional router handle, the candidate emit in the wake decision block, and the normalized-partial feed out of `_pull_revisions`; it still contains no cancel, flush, or duck token.
- **The interrupt policy is honored, never bypassed (ADR-0006 D8 :418-419).** `ResponseInterruptPolicy.confirmed_playback`'s default becomes `"interrupt_expected_playback_generation"` (jarvis/shared/realtime.py:202) and `start_response_run` issues the policy from that config-carried default (jarvis/decision/response_run.py:683-686). The runtime-side interrupt callable resolves its target from `ResponseRunRegistry.open_runs()`, reads that run's `interrupt_policy`, and acts only when `confirmed_playback` is `interrupt_expected_playback_generation` and `generation_action` is `cancel`; a policy of `ignore`, zero open runs, or more than one open run cancels nothing and returns that outcome string for the trace. The runtime never invents a scope.
- **One interrupt entry point.** A permitted confirmed barge-in calls the existing `make_response_cancel_callable(runtime)(response_id, "generation", "barge_in")` (jarvis/runtime/__init__.py:1646-1708) and nothing else. `"barge_in"` joins `RESPONSE_CANCEL_REASONS` (jarvis/shared/realtime.py:23-25) so the durable `response.cancelled` carries the true reason instead of being normalized to `operator_request`. Playback stop is whatever that cancel path already does once lane A's `_interrupt_active` CAS is on it; this card adds no new public method to `StreamingTTSPipeline` and does not touch `jarvis/surface/voice_media.py` at all.
- **Out-of-band priority.** The interrupt never enters the intent-pump queue (jarvis/runtime/inherent_loop.py:969); it is a direct in-memory call on the control path, the same shape the HTTP cancel route already uses (ADR-0008 §8 barge-in control lane). The interrupting utterance's own transcript still reaches L3 through `utterance.received` unchanged, so a confirmed interruption becomes the next full question (ADR-0006 D8 :419).
- **Mode is read, never resolved.** The allowed mode comes from B2's `DeviceProfileSnapshot.allowed_barge_mode` exposed on the session. `keyword_two_stage` enables the candidate and the keyword confirm; `ptt` leaves the wake suppression untouched so only a PTT upload can confirm; `natural` is treated as `keyword_two_stage` and logs one warning per snapshot change — natural stays unreachable in this card.
- **Config.** `realtime.single_audio_ingress.barge_in.{enabled: false, candidate_window_ms: 1200, confirm_timeout_ms: 800, interrupt_keywords: ["停", "别说了", "stop"]}` parsed by `realtime_input_session_config_from_mapping` into a `BargeInConfig` on `RealtimeInputSessionConfig` (jarvis/surface/voice_session.py:57-73), mirroring `PartialAsrConfig` (class at :47, mirror field :73). Values are D8/D9 candidates, not promises; `confirm_timeout_ms` is the outer bound on the injected callable and sits above the existing `realtime.response.cancel_timeout_ms: 500`. With `enabled: false` behavior is byte-identical to today: no candidate window, no router, no arming during output, no new field on any wire payload.
- **The two existing pins are revised, not deleted, to a stronger invariant:** `output_active` alone, a wake hit alone, and any VAD verdict alone can never cancel; a hard cancel happens only on `BargeInSignal(phase="confirmed")`, which requires a second independent signal (an interrupt keyword inside the window, or a PTT upload) and a permitting `ResponseInterruptPolicy`. The canary keeps its forbidden-token scan and both trace-string assertions (the off path and `ptt` mode still emit them) and gains `request_response_cancel` and `_interrupt_active` to the forbidden set. The wave-3 integration test keeps its existing case unchanged for `barge_in` off and gains a sibling with `barge_in.enabled: true` in `ptt` mode where a wake hit during output still arms nothing and the injected interrupt callable is never called.

## Affected contracts and files
- L5 `jarvis/surface/voice_interrupt.py` (new) — `BargeInPhase`, `BargeInSignal`, the candidate window, the keyword matcher, `BargeInRouter`, the three traces.
- L5 `jarvis/surface/voice_session.py` — `BargeInConfig` on `RealtimeInputSessionConfig` (:57-73) and its parser (def :1321); optional router handle on `DuplexVoiceSession` (:844-870); candidate emit and conditional arming in the wake decision block (:1036-1069); the normalized partial text surfaced out of `UtteranceAssembler._pull_revisions` (:772-802) to the router; module docstring (:7-11) corrected; barge-in counters on `VoiceSessionMetrics` (class at :181) and `metrics()` (def :1260).
- L5 `jarvis/surface/inherent_server.py` — optional `barge_in_confirm_callable` on `InherentDeps` (between `voice_pipeline_callable` :305 and `cancel_response_callable` :306, no field between them today), called from `_run_asr_submit` (def :515 to :597) before the ASR offload; absent by default so the route table and OpenAPI schema are unchanged.
- shared `jarvis/shared/realtime.py` — `"barge_in"` joins `RESPONSE_CANCEL_REASONS` (:23-25); `confirmed_playback`'s default becomes `interrupt_expected_playback_generation` (:202).
- L3 `jarvis/decision/response_run.py` — the interrupt-policy construction inside `start_response_run` (:683-686) and nothing else in the file.
- runtime `jarvis/runtime/__init__.py` — the barge-in interrupt callable that resolves the target from `open_runs()`, applies the run's policy, and delegates to `make_response_cancel_callable` (:1646-1708); the policy value passed at the `start_response_run` call site (:1601).
- runtime `jarvis/runtime/inherent_loop.py` — two wiring points only: the `DuplexVoiceSession` construction (:2128-2137) and the `InherentDeps` construction (:3022-3040) for the PTT confirm. No other line changes.
- `config/jarvis.yaml` — extend the existing `barge_in` block (:276-278) with this card's keys (`enabled: false` by default, `candidate_window_ms`, `confirm_timeout_ms`, `interrupt_keywords`); do not create a second block or move the existing one.
- `tests/integration/test_barge_in_two_stage.py` (new); revisions to `tests/canary/test_canary_audio_ingress_realtime_safety.py:83-98` and `tests/integration/test_wave3_single_audio_ingress.py:1660-1691`.
- `scripts/replay_barge_in.py` (new) — the live composition root.

## Boundaries and non-goals
- Layers that may change: L5 (`jarvis/surface`, excluding `voice_media.py`), `jarvis/shared/realtime.py` (only the two lines named above), `jarvis/decision/response_run.py` (only the `start_response_run` policy construction), runtime wiring, config, tests, scripts.
- This card launches only after cards `incremental-tts-from-permitted-segments` and `device-profile-resolver` are merged; the hub re-pins the cancel-path and `DeviceProfileSnapshot` line references at launch.
- ADR-0006 D8's `InterruptUtteranceRouter` — the closed deterministic router that final ASR text passes through for control phrases and the compound-utterance remainder split — stays unbuilt and keeps its name; this card's `BargeInRouter` is a different object that receives `BargeInSignal`s only.
- Must not change: `jarvis/execution/`; any file under `jarvis/decision/` other than the one `response_run.py` policy construction; `request_response_cancel`'s scope table — `foreground_output` stays unsupported; `ResponseInterruptPolicy.action_action`; `jarvis/surface/voice_media.py`; `_tts_watcher` and every other `jarvis/runtime/inherent_loop.py` line beyond the two wiring points; the single capture owner and subscriber structure; per-utterance Silero reset and the `record` profile used for all capture; the endpointing and partial-ASR decisions; the PTT `/inherent/asr-submit` request and response shapes; device-profile resolution (read only); any AEC claim.
- Non-goals: ducking of any kind — D8 phase 1's `duck_gain` ramp and F11's unduck are deferred, not rejected, to a later card; the `tts` Silero threshold profile (`_MODE_THRESHOLDS`, jarvis/surface/voice_audio.py:77-80) stays unused; enabling `natural` mode; D8's control-phrase grammar and remainder split; action cancellation of any kind; a `foreground_output` playback lease; the D9 acceptance thresholds and the §10.2 latency SLOs; real-microphone acceptance runs.

## Rejected approaches
- A VAD-based speech candidate during playback (the earlier two-phase draft) — without AEC the speaker's own audio produces speech verdicts constantly, and D9 (:466, :475) requires two *spoken* stages, not one spoken token plus an acoustic guess. Dropped entirely, including the `tts` threshold profile it would have used.
- A single keyword with no wake phrase (bare "停") — D9 rejects it explicitly (:475): Jarvis may say that word itself.
- Reusing `reason="user_stop"` to avoid touching the closed vocabulary — the barge-in row would be indistinguishable from a panel Stop press in the Event Log, which is what the live evidence has to tell apart.
- Routing the interrupt through the intent-pump queue — D8 exists because a stop utterance queued behind the response it should interrupt cannot stop it; ADR-0008 §8 requires an independent highest-priority lane.
- A new press/release wire op for PTT — the WAV upload already proves a deliberate press, needs no Swift change, and cannot be produced by echo.
- A new public `StreamingTTSPipeline.interrupt_active_speech` — lane A's cancel-path `_interrupt_active` CAS already stops playback from the durable cancel, and a second stop entry would race it for the same generation.
- Bypassing `ResponseInterruptPolicy` and always stopping — D8 :418-419 makes the runtime a mechanical applier of the L3-issued policy; making the default permit the interrupt is a one-value config change, while a bypass would make the frozen contract decorative.

## Acceptance evidence
- Positive (hermetic): raw pytest output of `tests/integration/test_barge_in_two_stage.py` covering — (1) a wake hit during output with barge-in armed in `keyword_two_stage` traces `barge_in_candidate`, arms capture, and calls the interrupt callable zero times; (2) an interrupt keyword in a partial revision inside the window emits exactly one `BargeInSignal` and calls the interrupt callable once with scope `generation` and reason `barge_in`; (3) a window that closes with no keyword traces `barge_in_candidate_dropped` and calls it zero times; (4) a PTT upload during output with no prior candidate confirms in both modes; (5) duplicate keyword revisions inside one window still produce exactly one signal; (6) in `ptt` mode a wake hit during output stays suppressed with the existing counter and trace, arms nothing, and no keyword can confirm; (7) `natural` is downgraded to `keyword_two_stage` with exactly one warning and never permits a natural confirm; (8) a target run whose `confirmed_playback` is `ignore` produces `barge_in_confirmed` with that outcome and zero cancels, and with `barge_in.enabled: false` there is no candidate, no router, and no new field on any wire payload.
- Positive (revised pins): raw output of `tests/canary/test_canary_audio_ingress_realtime_safety.py` and `tests/integration/test_wave3_single_audio_ingress.py` showing both revised tests passing, with the new invariant asserted in each.
- Live (required): raw output of `scripts/replay_barge_in.py`, built on the `scripts/replay_endpointing.py:123-172` composition root plus the real wake engine, a real `StreamingTTSPipeline`, and a real ResponseRun, feeding a WAV containing the wake phrase followed by an interrupt keyword through `FileReplayBackend` while a streamed answer is playing, with `barge_in.enabled: true` and `partial_asr.enabled: true` in `keyword_two_stage`. Canary values quoted from the run: the `response.cancelled` row with `reason="barge_in"` and its `response_id`; the `surface.playback_interrupted` row for the same generation with no later `surface.playback_segment_prepared`; and the `barge_in_candidate` / `barge_in_confirmed` trace pair with the elapsed ms between them. Default output is switched to `BlackHole 16ch` with `SwitchAudioSource -c -t output` captured first and restored afterwards on every path; if `SwitchAudioSource` or the device is absent, say so and record the live row as an owner follow-up with the exact command. Real-microphone runs are an owner follow-up, not this card's evidence.
- Regression: raw output of `PYTHONPATH=. .venv/bin/python -m pytest -q tests/integration/test_endpointing_partial_asr.py tests/integration/test_voice_file_replay.py tests/integration/test_wave3_single_audio_ingress.py` passing; the full hermetic suite `PYTHONPATH=. .venv/bin/python -m pytest -q -m "not live_llm and not live_codex"` compared against the integration count measured at launch plus this card's new tests, with any delta explained; `lint-imports`, `ruff check .`, `mypy --strict jarvis tests scripts tools` each exit 0.
- The 8006 daemon on `~/.jarvis-realtime-test` is never signalled or stopped; every artifact of this card lives under its own runtime root.

## Docs to sync
- `docs/adr/0006-full-duplex-voice-session.md` D8 (:412-438) — amend to what shipped: the wake hit during active output as the candidate, the configured interrupt keyword on the partial-ASR path as the confirm, the PTT upload as the direct confirm, no VAD candidate stage, the config keys, and the shipped `BargeInRouter` as the `BargeInSignal` receiver while D8's transcript-routing `InterruptUtteranceRouter` remains unbuilt and keeps its name. Record the phase-1 `duck_gain` ramp and the F11 unduck as **deferred, not rejected**, to a later card. Record that `confirmed_playback` now defaults to `interrupt_expected_playback_generation` so the runtime's mechanical application of the policy has an effect.
- `docs/adr/0006-full-duplex-voice-session.md` §10.1 (:743-771) — add `barge_in_candidate` and `barge_in_candidate_dropped`; `barge_in_confirmed` is already listed (:753).
- `docs/adr/0006-full-duplex-voice-session.md` D9 (:440-476) and §6 F11/F12/F18 (:651, :652, :658) — judged unchanged apart from F11's unduck being covered by the D8 deferral note; say so explicitly.
- `docs/adr/0008-real-time-response-streaming.md` D10 (:571-729) — record `barge_in` joining the closed `response.cancelled` reason vocabulary and the `confirmed_playback` default change. D10 does not enumerate the reason set in prose today (only jarvis/shared/realtime.py:23-25 does, citing "ADR-0008 D10, Q7"), so the amendment introduces the enumeration there. Judge the scope table unchanged: `generation` is reused, `foreground_output` and its playback lease are still absent, and that remains the stop-speech card's work.
- `docs/spec.html` — judged unchanged unless a §3 invariant changes; say so explicitly.

## Open questions
(none)

## /goal condition
Implement docs/goals/keyword-ptt-safe-barge-in.md on the current branch. The goal is met when all of the following appear in the transcript: (1) the diff adds jarvis/surface/voice_interrupt.py holding BargeInSignal and BargeInRouter, makes a wake hit during active output open a bounded candidate window instead of being dropped when barge-in is armed in keyword_two_stage, confirms only on a configured interrupt keyword found in a partial-ASR revision inside that window or on an /inherent/asr-submit upload during output, adds the config block realtime.single_audio_ingress.barge_in with enabled defaulting to false, adds "barge_in" to RESPONSE_CANCEL_REASONS, makes ResponseInterruptPolicy.confirmed_playback default to interrupt_expected_playback_generation with start_response_run issuing it, and routes every confirmed barge-in through the existing make_response_cancel_callable with scope generation and reason barge_in only when the target run's own policy permits it — with no file changed under jarvis/execution/, no file under jarvis/decision/ changed except the start_response_run policy construction, no change to jarvis/surface/voice_media.py, no new public StreamingTTSPipeline method, no _tts_watcher change, no foreground_output scope support, no VAD-based candidate, no ducking, and no AEC or natural-mode claim; (2) raw pytest output of tests/integration/test_barge_in_two_stage.py covering all eight cases listed under Acceptance evidence, ending in a pass line; (3) raw output of the revised canary and the revised tests/integration/test_wave3_single_audio_ingress.py, with a statement of the invariant each now pins — that output_active alone, a wake hit alone, and any VAD verdict alone can never cancel, and only a confirmed BargeInSignal under a permitting policy can; (4) raw output of the live replay run quoting the response.cancelled row with reason barge_in and its response_id, the surface.playback_interrupted row with no later surface.playback_segment_prepared, and the barge_in_candidate / barge_in_confirmed trace pair with the elapsed ms between them, together with SwitchAudioSource -c -t output before and after showing the same device restored — or, if the tool or BlackHole 16ch is absent, an explicit statement of that with the exact follow-up command recorded in Progress; (5) raw output of the three named regression files, and of the full hermetic suite with live tests excluded compared against the integration count measured at launch plus this card's new tests with any delta explained, plus lint-imports, ruff check ., and mypy --strict jarvis tests scripts tools, each ending with a pass line or exit 0; (6) a statement that the daemon on port 8006 with runtime root ~/.jarvis-realtime-test was never signalled or stopped; (7) ADR-0006 D8, §10.1, D9 and §6 F11/F12/F18, ADR-0008 D10, and docs/spec.html each updated or explicitly judged unchanged, with the D8 amendment naming BargeInRouter as the BargeInSignal receiver, stating that D8's transcript-routing InterruptUtteranceRouter stays unbuilt and keeps its name, and recording the phase-1 duck_gain ramp and the F11 unduck as deferred, not rejected — following the rule: update the canonical document that owns a changed contract, do not document what the code already makes clear, do not duplicate a fact across documents; (8) each slice committed with the project commit skill and git status clean; (9) a Progress line per slice in the card. Or stop after 70 turns.

## Progress

- Slice 1 (`1bf614e`) — `barge_in` joins `RESPONSE_CANCEL_REASONS`, `ResponseInterruptPolicy.confirmed_playback` defaults to `interrupt_expected_playback_generation` and `start_response_run` issues it, and `make_barge_in_interrupt_callable` resolves the single open run from `open_runs()`, applies that run's policy, and delegates to `make_response_cancel_callable(response_id, "generation", "barge_in")`. Hermetic 912 passed / 64 deselected (unchanged from the launch baseline at `cff12e1`); lint-imports 1 kept 0 broken; ruff clean; mypy strict clean (226 files).
- Slice 2 (`3b6af31`) — `jarvis/surface/voice_interrupt.py` (BargeInConfig/BargeInSignal/BargeInRouter, three traces, bounded interrupt call), the wake-block candidate emit plus normalized-partial feed and PTT confirm in `voice_session.py`, `barge_in_confirm_callable` on `InherentDeps`, both `inherent_loop` wiring points, and the extended `barge_in` config block. Hermetic 912 passed / 64 deselected (unchanged: the new paths are all off by default); lint-imports 1 kept 0 broken; ruff clean; mypy strict clean (227 files).
- Slice 3 (`0cbb9db`) — `tests/integration/test_barge_in_two_stage.py` (18 cases across router / session-with-threads / runtime altitudes, covering all eight Acceptance-evidence cases), the revised canary (`voice_interrupt` added to the scanned source, `request_response_cancel` and `_interrupt_active` added to the forbidden set, both trace strings kept), and the wave-3 `ptt`-mode sibling. Hermetic 931 passed / 64 deselected = 912 launch baseline + 18 new + 1 sibling; regression trio 111 passed; lint-imports 1 kept 0 broken; ruff clean; mypy strict clean (228 files).
- Slice 4 (`63c385c`) — `scripts/replay_barge_in.py` and the live run. Raw canaries: `barge_in_candidate` window_id=1 `allowed_barge_mode=keyword_two_stage` probability=0.962 → `barge_in_confirmed` confirm_source=keyword keyword=别说了 outcome=cancelled, **734.87 ms** apart; `response.cancelled` `reason=barge_in` `cancel_scope=generation` `response_id=RESP6a4b654f0bc2482f87ba90c3a66a5b05`; `surface.playback_interrupted` `reason=response_cancelled` `playback_generation_id=1` with no later `surface.playback_segment_prepared`; the interrupting utterance still committed as `utterance.received` transcript `别说了。`. `SwitchAudioSource -c -t output` = `MacBook Pro Speakers` before and after. Per the hub's 2026-09-05 rule the guard restores to `MacBook Pro Speakers` when the pre-run route is already the loopback, so a crossed run cannot leave the machine silent; the restore runs in a `finally`. The live run used `candidate_window_ms: 2500` (shipped default stays 1200) — the measured 734.87 ms candidate→confirm is the calibration datum for tightening it. Inputs kept at `~/.jarvis-lane-b-test/barge-in/` (`barge_final.wav` = MiniMax-rendered `Hey Jarvis，别说了。`, wake prob 0.962; `barge_in_overlay.yaml`; `one_clip.py`).
- Owner follow-up (not a blocker): a real-microphone barge-in run. Exact command once a mic recording exists — `set -a; source ~/.jarvis/env; set +a; PYTHONPATH=. .venv/bin/python scripts/replay_barge_in.py --wav <recorded.wav> --config ~/.jarvis-lane-b-test/barge-in/barge_in_overlay.yaml --runtime-root ~/.jarvis-lane-b-test/barge-in/rt`.
- Slice 5 — docs sync (`1919d9e`) plus the verifier pass. Deliberate deviation, stated: the card lists "the policy value passed at the `start_response_run` call site"; I added a keyword-only `confirmed_playback` parameter to `start_response_run` defaulting to the dataclass default and did NOT add a pass-through at `jarvis/runtime/__init__.py:1601`, because no config key carries a different value. The parameter's only consumer is the case-8 test, which in exchange exercises the real production constructor rather than a hand-built run — a test-driven choice, not a production knob.
- Verifier fixes — three confirmed items. (a) ADR-0006 D8 now records the three boundaries of naming the target from `open_runs()`: a run unregistered at generation end cannot have its playback tail stopped (`no_open_run`; that needs the deferred `foreground_output` lease), the target is the single open run rather than the owner of the current playback generation, and a dropped candidate still commits its speech as an ordinary turn so speaker self-wake can start an echo turn without AEC. (b) `BargeInRouter.open_candidate` now counts and traces a live window replaced by a newer wake hit as `barge_in_candidate_dropped reason=superseded_by_candidate`, so ADR-0006 §6 F11's "false candidates are counted" holds for that path too. (c) `InherentDeps.barge_in_confirm_callable` is left unbound when the session has no router, so `enabled: false` really is byte-identical on the `/inherent/asr-submit` path instead of a no-op thread hop. Judged and not changed: the canary's two new string-presence assertions (the behavioral pin lives in the integration test; the canary's value is the forbidden-token scan), and D8's older phase-2 ordering prose, which describes lane A's playback-stop path rather than this card's.

# Goal: device-profile-resolver

## Goal
The realtime input path knows which audio route it is on (input and output device identity and class), re-resolves that profile on any route change through the existing fault and epoch path, and exposes an `allowed_barge_mode` that is never natural unless the profile is explicitly accepted, so every later barge-in decision fails closed by construction.

## Why
ADR-0006 D9 makes barge-in mode a function of the device profile and F18 requires a route change to revoke natural mode before reopening. Today nothing knows the output route and nothing can revoke anything, which blocks keyword barge-in and natural mode alike.

## Current behavior
- Base: lane/b after `git merge realtime-integration` (which carries errata 1db1386 and this card); card 1 endpointing-partial-asr is already on lane/b.
- Input device is the system default resolved through sounddevice with no name matching or preference (jarvis/surface/voice_backend.py:_default_input_device_profile, :266-283); output streams open with no device identity or classification (jarvis/surface/voice_tts.py:517-540).
- Route change detection is poll-only and input-only: `AudioIngress._run_worker` compares `backend.current_device_uid()` every `route_poll_s` (config/jarvis.yaml:241, default 1.0) and raises `BackendFault("default_device_changed")` on mismatch (jarvis/surface/voice_audio.py:1585-1610). `notify_route_change` body is voice_audio.py:1979-1990; no production caller; one test caller tests/integration/test_wave3_single_audio_ingress.py:2357. No CoreAudio listener exists.
- `BackendCapabilities` are hard-coded `aec=False, natural_barge_in=False` (jarvis/surface/voice_backend.py:474-479); `DuplexVoiceSession.start` traces `natural_barge_in_enabled=False` (jarvis/surface/voice_session.py).
- `InputCapabilityState` has seven values and one fault entry point: `_handle_fault` (jarvis/surface/voice_audio.py:1719) is already the sole fault entry, reached from the fault poll (:1584), the route poll (:1594/:1605) and `notify_route_change` (:1984).
- No `DeviceProfileKey`, `DeviceProfileSnapshot`, `AudioRouteObserver`, `route_kind`, or `allowed_barge_mode` exists anywhere; the config `single_audio_ingress` block has no `barge_in` or profile keys (config/jarvis.yaml:220-248; :197-214 is `streaming_output`).
- The Inherent broadcaster already has a `voice_capability` op, emitted by `broadcast_voice_capability` (jarvis/surface/inherent_output.py:286-320, payload :299-308), replayed to new clients at :116-124, called from `_capability_changed` (jarvis/runtime/inherent_loop.py:2070-2092), carrying the input capability snapshot; the existing trace for capability changes is `audio_input_capability_changed` (jarvis/surface/voice_audio.py:2020-2030). ADR §10.1 names no route trace.
- `_spawn_voice_input_owners` selects the single input owner from static config only (jarvis/runtime/inherent_loop.py:2174-2206).

## Target behavior
- ADR-0006 D9 (branch text §3 D9) is the contract for the software half: `DeviceProfileKey` = D9's seven fields `input_uid, output_uid, backend, route_kind, aec_mode, input_sample_rate, output_sample_rate` with `aec_mode` fixed to `none` in this card, and `DeviceProfileSnapshot` = `key, stream_epoch, validation_record_hash (optional), allowed_barge_mode`, resolved at stream open and on every route change; F17/F18 in §6 govern downgrade: a route change closes the failed epoch through `_handle_fault`, revokes any barge mode above the new profile's allowance before reopen, and publishes the new snapshot.
- Route kinds are conservative: `headphones` only when the CoreAudio transport type (`kAudioDevicePropertyTransportType`) is BuiltIn and the data source (`kAudioDevicePropertyDataSource`) is the headphone jack (`hdpn`), or when the exact profile key is listed in `accepted_natural_profiles`; Bluetooth and USB transports resolve to `unknown` (a USB DAC speaker and a USB headset share one transport type); `speaker` for built-in or line-out output that is not the headphone jack; `unknown` otherwise. Route kind values are `headphones | speaker | hardware_aec | unknown` (`hardware_aec` is never produced by this card). `unknown` resolves like `speaker`.
- `allowed_barge_mode` is `ptt` or `keyword_two_stage` from config, and `natural` only when the profile key is listed under an explicit accepted-profiles config list; that list ships empty, so natural is unreachable in this card. `BackendCapabilities.aec` stays False everywhere.
- The output-uid accessor added to `AudioDuplexBackend` (voice_backend.py:192-240) defaults to returning None, which resolves `unknown`; card 1's file-replay backend and the test `_FakeBackend` (tests/integration/test_wave3_single_audio_ingress.py:53) implement it by returning None.
- The route observer polls both default input and default output identities on the existing `route_poll_s` cadence (a CoreAudio default-device listener may replace polling only if it feeds the same `notify_route_change` path); an output route change with unchanged input still produces a new stream epoch and a re-resolved snapshot, per F18.
- The snapshot and mode are visible: a new `audio_route_changed` trace point (previous and next profile key, reason) and the existing `voice_capability` broadcast gains `route_kind` and `allowed_barge_mode` fields; the Swift v1 client ignores unknown fields.
- Config keys under `realtime.single_audio_ingress`: `route_observer.enabled` (default false: no output polling, behavior identical to today), `barge_in.detection_mode` (`ptt` default, `keyword_two_stage`), `barge_in.accepted_natural_profiles` (list, default empty). Unknown or malformed values fail closed to `ptt`.
- PTT upload continues to work in every profile and during a route change.

## Affected contracts and files
- L5 jarvis/surface/voice_backend.py — profile key/snapshot types, output device identity and transport classification, capability plumbing.
- L5 jarvis/surface/voice_audio.py — route observer loop on both devices, `notify_route_change` becomes the single entry, trace; `route_observer.enabled` and its keys parsed into `AudioIngressConfig` (:417-427, parser :2330-2398); observer runs on the existing `route_poll_s` cadence inside `_run_worker`.
- L5 jarvis/surface/voice_session.py — snapshot exposure on the session.
- L5 jarvis/surface/inherent_output.py — two additive fields on `voice_capability`.
- runtime jarvis/runtime/inherent_loop.py — unchanged; the runtime starts nothing new.
- config/jarvis.yaml — the three keys, defaults off/ptt/empty.
- tests/integration — fake-backend route change harness; data-driven classification table.

## Boundaries and non-goals
- Layers that may change: L5 (`jarvis/surface`), runtime wiring, config, tests.
- Must not change: the single capture owner and owner registry; PTT `/inherent/asr-submit`; VAD, endpointing, or wake code; any claim of AEC; `natural_barge_in` remaining False in capabilities; v1 wire ops beyond additive fields; L3; the CoreAudio ctypes shim stays L5-owned inside voice_backend.py and must not import jarvis/deployment/sleep_wake.py or any L6 module.
- Non-goals: the hardware or VoiceProcessingIO AEC spike; keyword barge-in detection itself; enabling natural mode; the D9 acceptance thresholds (they gate a later "accept profile" decision, not this card); playback-side handling of an output route change (ADR-0006 F10, and F18's playback-generation interrupt/reopen): this card re-resolves the profile and the input stream_epoch only; the OutputStream (opened with device=None, jarvis/surface/voice_tts.py:647/:735) is untouched.

## Rejected approaches
- Name-based headphone detection alone (for example matching "AirPods") — a renamed or unknown device would be granted headphone privileges; transport type is the evidence, names are at most a hint.
- A second observer path outside `_handle_fault` — `_handle_fault` (jarvis/surface/voice_audio.py:1719) is already the sole fault entry, reached from the fault poll (:1584), the route poll (:1594/:1605) and `notify_route_change` (:1984); a parallel path would race the epoch.
- Defaulting `route_observer.enabled` to true — output polling is new behavior and the card must be byte-identical to today when off.

## Acceptance evidence
- Positive (hermetic): an integration harness with a fake `AudioDuplexBackend` shows raw pytest output for: profile resolved at open with the expected key; an output-only uid change produces `audio_route_changed`, a new stream epoch through `_handle_fault`, and a re-resolved snapshot while the input uid is unchanged; an input change does the same; `allowed_barge_mode` is `ptt` for `speaker` and `unknown` regardless of `detection_mode` other than `keyword_two_stage`, and never `natural` even when `detection_mode` is set to an unsupported value; a profile listed under `accepted_natural_profiles` resolves `natural` only for `headphones`; a data-driven table of (input transport, output transport, name) to `route_kind` passes; the `voice_capability` payload carries the two new fields.
- Regression: `PYTHONPATH=. .venv/bin/python -m pytest -q tests/integration/test_wave3_single_audio_ingress.py tests/integration/test_inherent_server_asr_submit.py tests/integration/test_voice_vad_endpoint.py tests/integration/test_endpointing_partial_asr.py` passes unchanged, including a regression assertion that `capabilities().aec` and natural barge-in stay False; with `route_observer.enabled: false` the existing route-change-during-open test behaves as before; full hermetic suite, `lint-imports`, `ruff check .`, `mypy --strict jarvis tests scripts tools` exit 0. Raw output shown.
- Live: start a daemon from this worktree on its own runtime root and port with `route_observer.enabled: true`; `brew install switchaudio-osx` if `SwitchAudioSource` is absent; `SwitchAudioSource -t output -s "BenQ PD2700U"`, observe the route trace and the new stream_epoch, then `SwitchAudioSource -t output -s "MacBook Pro Speakers"` and observe the second re-resolution; the original default output must be restored before the run ends. If the install fails, record the live row as an owner follow-up in Progress and do not block.

## Docs to sync
- ADR-0006 D9 judged unchanged; ADR-0006 §10.1 gains the route trace line.
- docs/spec.html — judged unchanged unless §3 names the capability broadcast fields; say so.

## Open questions
(none)

## /goal condition
Implement docs/goals/device-profile-resolver.md on the current branch. The goal is met when all of the following appear in the transcript: (1) the diff adds `DeviceProfileKey`, `DeviceProfileSnapshot`, route-kind classification by transport type, an input-plus-output route observer feeding `notify_route_change`, `allowed_barge_mode` resolution with natural unreachable by default, the `audio_route_changed` trace, the two additive `voice_capability` fields, and the three config keys with off/ptt/empty defaults, with no change to the capture owner, PTT path, VAD/endpointing/wake code, or any AEC claim; (2) raw pytest output of the new harness covering the seven cases in Acceptance evidence, ending in a pass line; (3) raw output of the four named regression files, the full hermetic suite with live tests excluded, `lint-imports`, `ruff check .`, and `mypy --strict jarvis tests scripts tools`, each ending with a pass line or exit 0; (4) the live route-switch trail using `SwitchAudioSource -t output -s "BenQ PD2700U"` then `SwitchAudioSource -t output -s "MacBook Pro Speakers"` showing `audio_route_changed` and a new stream epoch for each switch and the original default output restored, or (if the `switchaudio-osx` install fails) an explicit statement recording the live row as an owner follow-up in Progress; (5) ADR-0006 D9, §6, §10.1 and docs/spec.html each updated or explicitly judged unchanged, following the rule: update the canonical document that owns a changed contract, do not document what the code makes clear, do not duplicate a fact across documents; (6) each slice committed with the project commit skill and `git status` clean; (7) a Progress line per slice in the card. Or stop after 60 turns.

## Progress

- profile key, route classification, resolver, config — 5e7cd4d — RouteKind/DeviceProfileKey/DeviceProfileSnapshot, CoreAudio transport shim, `route_observer`/`barge_in` keys default off/ptt/empty, ADR-0008 §6 comment reworded; table + resolver + config cases.
- input-plus-output route observer, snapshot, trace, capability fields — 6605525 — `audio_route_changed`, F18 revoke in `_handle_fault`, `voice_capability` gains route_kind/allowed_barge_mode (2-line runtime pass-through); new file 29 passed, regression four 115 passed, full suite 782 passed.
- live route switch — (no sha) — daemon on 8016 with route_observer on; BenQ PD2700U not connected, "BlackHole 16ch" used instead. Attempt 1 (Speakers → BlackHole): `audio_input_fault output_route_changed`, epoch 2 minted, but the PortAudio reopen exceeded `backend_open_timeout_s` 2.0 s (`audio_input_stream_open_failed bounded_timeout_state_uncertain`) so the ingress went `close_uncertain` and the second switch could not be observed. Attempt 2 (daemon started with BlackHole as default, then → Speakers): startup profile `…|BlackHole16ch_UID|sounddevice|unknown|…` epoch 1, then `audio_route_changed` previous BlackHole/unknown → next `BuiltInSpeakerDevice|speaker`, epoch 2 opened in 361 ms, capability available route_kind=speaker allowed_barge_mode=ptt; original default output restored. Owner follow-ups: repeat with the BenQ PD2700U when connected; decide whether a reopen after an output switch needs a longer open bound or a late-open recovery (F17 uncertain-open path, pre-existing).
- docs sync — see the docs commit — ADR-0006 §10.1 gains `audio_route_changed`; D9, §6, docs/spec.html judged unchanged.

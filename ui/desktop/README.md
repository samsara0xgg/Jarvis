# ui/desktop/

Electron pet-mode surface — ambient on-desktop presence.

## Status

Reserved slot. The legacy desktop pet-mode lived at `~/Projects/jarvis-legacy/
desktop/`. A redesign on top of the new L5 contracts (`AttentionRouting`,
`PresentationIntent`, `UserResponse`) lands post-M5; this directory exists in
the tree so the layer story is complete.

Will house its own JS / Electron build (independent of the Python package).
